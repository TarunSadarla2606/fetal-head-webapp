{
  "name": "fetal-head-webapp",
  "version": "0.1.0",
  "private": true,
  "description": "FetalScan AI clinical decision support — Next.js frontend (Batch 2+)",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
